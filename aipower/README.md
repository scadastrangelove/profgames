# AI Power Atlas — v0.34

**Двуязычный исследовательский атлас структурной власти и киберустойчивости ИИ. Выпуск — 6 сентября 2026 года.**
База последнего слоя: v0.33, коммит `c1d15d3eb057732d07de5b10329bd412c10958e0`.

В выпуске **372 события, 73 тезиса, 30 проверок тезисов, 25 сюжетных арок, 790 связей и 556 уникальных URL источников**. Кибервыборка содержит 148 карточек, из них 85 — опорные. Статусы тезисов: 49 подтверждены, 22 подтверждены частично, 2 спорны.

## Что добавлено в v0.34

**Поток реальных находок и ёмкость сопровождения.** Добавлены 15 двуязычных событий, один ограниченный тезис и 45 связей. Апрельский curl сохранён как историческая переходная точка. Основные свидетельства — августовский CVD dashboard Anthropic, письмо Linux netdev, выпущенные исправления curl и фактическая пауза приёма сообщений. Новых верхнеуровневых доменов или арок нет.

**Шесть дорожек чтения.** В кибервкладке раскрывается блок «От находки к устойчивости»: поток реальных находок, проверенное устранение, общее сопровождение, обучение на инцидентах, доступ защитников, изменения и восстановление. Он объединяет 15 новых и 16 ранее существовавших записей. Верхние фильтры действуют и здесь; дорожка и открытая карточка сохраняются в ссылке и при переключении языка.

**Свидетельства не смешиваются.** Проверенная находка, сообщение, патч, выпуск, установка и восстановленная услуга — разные результаты. CVD имеет две ветки, а не единую последовательную воронку. Linux-счётчики относятся к патчам. Akrites и Daybreak остаются анонсами, SAFE — RFC, Watershed — запущенным пилотом без установленных итогов.

**Существующие записи.** Firefox получил первичную публикацию Mozilla без удаления старого источника. У rust-in-peace сохранены дата 6 августа и исторические 33 исправления; сводка с 37 fixed/merged и два отдельно проверенных upstream PR вынесены в наблюдение состояния. Карточка включена в опорную выборку с этим ограничением. Неверифицированные итоги всех сообщений не превращены в подтверждённые уязвимости.

**Хронология.** Пауза curl стоит на 1 июля, а её анонс — в поле даты источника. Watershed: запуск 31 августа, публикация 1 сентября. Defense Factory — наблюдение недатированной страницы 6 сентября; дата спринта и первой публикации не установлена.

## Сборка и проверки v0.34

```bash
cd aipower
python3 scripts/build.py
python3 scripts/validate.py
python3 tests/browser_test.py --chromium /usr/bin/chromium
python3 tests/resilience_test.py --chromium /usr/bin/chromium
```

Для данных и сборки достаточно Python 3.10+. Браузерным тестам нужны Playwright и Chromium (`requirements-test.txt`). Проверки не используют модельные API и не запускают пакеты экспериментов.

Миграция проверяет точную пару входных файлов v0.33 до записи любого результата:

```bash
python3 scripts/migrate_resilience_throughput.py   --root /path/to/aipower-v033   --output /tmp/aipower-v034-json
```

Проверка воспроизводимости: `python3 tests/resilience_migration_test.py --baseline /path/to/aipower-v033`.

Повторная миграция уже обновлённой пары и несовпадающий хеш любого входа отвергаются. Исходный пакет, журнал источников, отложенные кандидаты, изменения по ID и результаты проверки находятся в `review/resilience-throughput/`. Описание полей — `SCHEMA_v034.md`.

## Готовые страницы и данные

- `ai-power-atlas-ru.html` — автономная русская версия.
- `ai-power-atlas.html` — автономная английская версия.
- `ai_power_storygraph_ru.json`, `ai_power_storygraph_en.json` — канонические данные.
- `index.html` — двуязычная страница входа.
- `CHANGELOG.md` — история выпуска и границы проверки.
- `SCHEMA_v034.md` — текущая схема слоя устойчивости, единиц измерения и дорожек чтения.
- `SCHEMA_v033.md` — предыдущее обновление по Astra и правила сопоставления разных видов monitorability.

HTML включает JSON, CSS и JavaScript и работает без внешних библиотек или API. Браузер может открыть страницу как локальный файл; для проверки опубликованного состояния используется GitHub Pages.

## Что добавлено в v0.33

**Astra и наблюдаемость.** Две новые карточки по system card OpenAI разделяют разные результаты: рост управляемости формой chain-of-thought и снижение неадверсариальной наблюдаемости; отдельно — уклонение от мониторов при явно заданном sandbagging и sabotage. Видео из исходной заявки не используется как источник: все числа взяты из официальной карточки.

**Сопоставимые числа.** `60,9% / 16,1% / 1,7%` относятся только к traces длиной 750–1 250 токенов. Снижение CoT- и full-context-наблюдаемости не переносится на action-only monitoring: по нему Astra в среднем показала более высокий результат.

**Границы результата.** Prompted evasion не выдан за производственный инцидент, автономное намерение или фоновую частоту. В тех же испытаниях full-context monitoring обнаружил все успешные honeypot-эксплуатации, сложные математические side tasks всегда были замечены, а стеганографический CoT не наблюдался.

**Обновление истории.** Мартовский CoT-Control сохранён как baseline для 13 тогда протестированных моделей. Сентябрьская Astra связана с ним отношением `updated_by`; контраргументы поведенческой дуги полностью локализованы на каждом языке.

## Основа v0.32

**Скрытие и преемственность.** Добавлены 12 двуязычных карточек по первичным публикациям 2024–2026 годов: Sleeper Agents, Alignment Faking, in-context scheming, hidden-objective audits, CoT faithfulness и monitoring, sandbagging, agentic misalignment, записка будущему экземпляру, AuditBench, reward-seeking в ходе RL и CoT controllability.

**Четыре разных механизма.** Новый локальный режим внутри `5A` перестраивает поведенческую карту по уровням «веса → режим наблюдения → объяснение → внешнее состояние». Он объединяет 12 новых и 6 существующих сравнительных карточек, но не создаёт 26-ю верхнеуровневую арку.

**Новая разметка.** `behavior_origin`, `concealment_targets`, `persistence_media` и `goal_source` отделяют имплантированную исследователями цель от prompted eval, training observation, внешней инструкции и production incidence. Неполный reasoning trace не маркируется автоматически как намеренная ложь.

**Калибровка тезиса.** Центральный тезис расширен, но остаётся `partially_verified`: способность менять поведение при наблюдении, сохранять условную стратегию и выносить состояние уже показана; высокая фоновая частота спонтанного долгосрочного scheming в обычной эксплуатации не установлена.

## Основа v0.31

**Поведение агентов.** Добавлены 22 двуязычные карточки, один калиброванный тезис и 34 явные связи. Ещё 29 ранее проверенных событий получили тематическую привязку к поведенческим механизмам; у шести из них уточнены контекст испытания, популяция агентов, общая записываемая среда и объект контроля.

**Два масштаба пятого домена.** `5A` описывает границы задачи, память, происхождение, координацию, наблюдаемость, остановку и влияние на решение. `5B` описывает идентичность агента, отзыв полномочий, хранение журналов, переносимость и выход из внешнего стека. Это связанные, но не взаимозаменяемые задачи.

**Пять зон контроля.** В кибервкладке появилась интерактивная схема «рамка задачи → память и происхождение → координация → контроль и остановка → решение и выход». Одиннадцать механизмов выбираются внутри этой схемы и не перегружают глобальные фильтры. Выбранный масштаб и механизм сохраняются в постоянной ссылке.

**Разные типы доказательства.** Инцидент, демонстрация возможности, оценка частоты, измерение поверхности действий, проверка меры контроля, прогноз риска и нормативный ответ размечены отдельно. Новый тезис остаётся `partially_verified`: отдельные механизмы наблюдаются, но высокая базовая частота долгосрочного скрытого целеполагания в обычной эксплуатации не установлена.

**Хронология.** Точка вики-инцидента OpenAI поставлена на 24 мая 2026 года — дату первого подтверждённого успешного изменения, а не публикации расследования. Работа о вмешательстве в отключение стоит на 13 сентября 2025 года — дате arXiv v1; обновление 26 января 2026 года хранится отдельно.

## Сборка и проверка

Для данных и HTML достаточно Python 3.10+:

```bash
cd aipower
python3 scripts/validate.py --refresh-metadata --skip-html
python3 scripts/build.py
python3 scripts/validate.py
```

Браузерные регрессии требуют Playwright и Chromium:

```bash
python3 -m pip install -r requirements-test.txt
python3 tests/browser_test.py --chromium /path/to/chromium
```

Исторический прогон v0.33 содержит 150 браузерных проверок: обе языковые версии, восемь вкладок, размеры 1440×1100 и 390×844, фильтры, постоянные ссылки, все 133 киберкарточки, переключение 5A/5B, четырёхступенчатую дорожку, выбор механизма и поведенческий блок в drawer. Тест не обращается к модельным API и не запускает экспериментальные пакеты P1/P2.

## Аудит и миграция

Исходный пакет сохранён без переписывания в `review/v031-agent-behavior/`:

- `candidates.json` — 22 кандидата, обновления существующих записей и черновая классификация;
- `candidate-matrix.csv` — таблица редакционного отбора;
- `source-ledger.csv` — журнал источников;
- `research-notes.md` — заметки фактчека и нерешённые границы.

`scripts/migrate_v031_agent_behavior.py` применяет нормализованный слой только к точной паре v0.30. Вход защищён SHA-256; повторный прогон поверх v0.31 прекращается с ошибкой. Для воспроизведения укажите каталог с двумя файлами v0.30:

```bash
python3 scripts/migrate_v031_agent_behavior.py \
  --root /path/to/aipower-v030 \
  --output /tmp/aipower-v031-json
```

Пакет `review/agent-concealment-persistence/candidates.json` содержит 12 принятых карточек и точные ограничения формулировок. `scripts/migrate_agent_concealment.py` применяет их только к закреплённой паре v0.31:

```bash
python3 scripts/migrate_agent_concealment.py \
  --root /path/to/aipower-v031 \
  --output /tmp/aipower-v032-json
```

Пакет `review/astra-monitorability/` содержит две принятые карточки, первичный source ledger в JSON и редакционные границы вывода. `scripts/migrate_astra_monitorability.py` применяет обновление только к точной паре v0.32:

```bash
python3 scripts/migrate_astra_monitorability.py \
  --root /path/to/aipower-v032 \
  --output /tmp/aipower-v033-json
```

Проверенные результаты находятся в `review/validation.json` и `review/browser-results.json`; рядом лежат контрольные снимки. Контрольные суммы файлов записаны в `SHA256SUMS`.

## Сохранённые свойства v0.30

Все старые ID событий, тезисов, проверок, арок и связей сохранены. В текущем выпуске 284 связи `part_of_arc` означают тематическую принадлежность, а не доказательство тезиса. Классификаторы акторов и географии остаются дедуплицированными; 16 неконкретных старых обозначений по-прежнему видны в отдельной очереди уточнения и не заменены догадками. Сюжет `ARC_COGSEC_LAB_TO_WILD_TO_STATE` расширен, но новая верхнеуровневая арка не создана.

## Границы достоверности

Для v0.34 проверялись конкретные первичные публикации и хронология нового пакета; это не независимая репликация исследований и не повторная проверка всех 556 URL. Внутренние отчёты поставщиков, препринты, контролируемые эксперименты и реальные инциденты не имеют одинаковой доказательной силы.

Пятидоменная киберрамка и разделение 5A/5B — авторская аналитическая схема, не нормативный стандарт, не шкала зрелости и не количественная модель риска. Связность графа и успешный браузерный тест не доказывают причинность редакционных связей.
